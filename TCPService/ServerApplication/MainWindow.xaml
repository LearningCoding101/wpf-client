<Window x:Class="ServerApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServerApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Logs -->
        <TextBox x:Name="LogTextBox" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Auto" IsReadOnly="True"/>

        <!-- Clients -->
        <ListBox x:Name="ClientsListBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Message Input -->
        <TextBox x:Name="MessageTextBox" Grid.Row="1" Grid.Column="0" Margin="5"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
            <Button x:Name="StartButton" Content="Start Server" Margin="5" Width="100" Click="StartButton_Click"/>
            <Button x:Name="SendButton" Content="Send" Margin="5" Width="100" Click="SendButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
